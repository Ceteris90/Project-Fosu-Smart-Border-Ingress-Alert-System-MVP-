# Template only. Do not apply or commit populated secrets.
# Prefer Azure Key Vault CSI/Workload Identity or a protected CI/CD pipeline.
apiVersion: v1
kind: Secret
metadata:
  name: fosu-app-secrets
  namespace: project-fosu
type: Opaque
stringData:
  DATABASE_URL: "postgresql+psycopg2://USER:PASSWORD@PRIVATE_POSTGRES_FQDN:5432/fosu?sslmode=require"
  FOSU_DASHBOARD_USERNAME: "fosu.admin"
  FOSU_DASHBOARD_PASSWORD_HASH: "scrypt$..."